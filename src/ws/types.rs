//! Transport-neutral websocket types.
//!
//! This module keeps the legacy path `defi_ws::ws::types::*` working while
//! moving the canonical definitions to `crate::core`.

pub use crate::core::types::*;

